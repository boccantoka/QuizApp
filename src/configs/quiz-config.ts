import { QuestionDifficulty, QuestionType } from '../services/quiz';

export const quizApiConfig = {
  questionDifficulty: QuestionDifficulty.Hard,
  questionType: QuestionType.Boolean,
  questionNumber: 10,
};
